version: 1.0
runtime: docker

build:
  dockerfile: Dockerfile
  context: .

run:
  network:
    port: 8501
    env: APP_PORT
  env:
    - name: STREAMLIT_SERVER_PORT
      value: "8501"
    - name: STREAMLIT_SERVER_ADDRESS
      value: "0.0.0.0"
