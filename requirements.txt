# ─────────────── Core data / HTTP ───────────────
pandas>=2.2
feedparser>=6.0          # robust RSS parsing
httpx[http2]>=0.28       # async HTTP client
tenacity>=8.2            # retry/back-off wrappers
pydantic>=2.9            # required by httpx for strict types

# ─────────────── NLP / ML ───────────────
scikit-learn>=1.4        # TF-IDF + misc utilities
transformers>=4.41       # HuggingFace pipelines
sentence-transformers>=2.7
spacy>=3.7               # stakeholder NER
spacy-transformers>=1.3  # transformer-based NER pipeline
torch>=2.2.0+cpu ; platform_system!="Linux" or platform_machine=="x86_64"
torch>=2.2.0      ; platform_system=="Linux" and platform_machine!="x86_64"
# ↑ swap the CPU wheel for +cu118 etc. if you have CUDA

# ─────────────── Vector store ───────────────
faiss-cpu>=1.7

# ─────────────── LLM client ───────────────
openai>=1.25             # GPT-4o api

# ─────────────── Streamlit UI ───────────────
streamlit>=1.37
rich>=13.7               # colourful logging (optional)

# ─────────────── Utility & caching ───────────────
python-dotenv>=1.0
pyyaml>=6.0
tqdm>=4.66
diskcache>=5.6           # used by disk_cache.py decorator
tabulate>=0.9            # pretty CLI tables
pytest>=8.0              # test suite

# ─────────────── (optional) scenario notebook ───────────────
# hdbscan>=0.8          # clustering - requires MS C++ Build Tools on Windows
# umap-learn>=0.5
# notebook>=7.4
